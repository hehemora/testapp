<%= form_with model: @article, url: cms_article_path(@article.id), method: "delete" do |f| %>
  <div>
    <%= csrf_meta_tags %>
      <% if @article.errors.any?%>
      <div class=”alert alert-danger”>
          <% @article.errors.full_messages.each do |message| %>
          <div><%= message %></div>
          <% end %>
      </div>
    <% end %>
    <input type="submit", class="nm-btn delete", value="Удалить статью" />
</div>
<% end %>