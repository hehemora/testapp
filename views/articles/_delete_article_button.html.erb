<%= form_with model: @article, url: article_path(@article.id), method: "delete" do |f| %>
  <div>
    <%= csrf_meta_tags %>
      <% if @article.errors.any?%>
      <div class=”alert alert-danger”>
          <% @article.errors.full_messages.each do |message| %>
          <div><%= message %></div>
          <% end %>
      </div>
    <% end %>
  <p>
    <%= f.submit 'Удалить статью', class: "button" %>
  </p>
</div>
<% end %>